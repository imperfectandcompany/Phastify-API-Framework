<!-- <?php
// // for bitwise operations in the future
// class PermissionUtil {
//     const PERMISSION_READ = 1;
//     const PERMISSION_WRITE = 2;
//     const PERMISSION_DELETE = 4;

//     public static function hasPermission($userRole, $requiredPermission) {
//         return ($userRole & $requiredPermission) === $requiredPermission;
//     }
// }

// $userRole = 1; // Assume ROLE_USER
// $requiredPermission = PermissionUtil::PERMISSION_READ;

// if (PermissionUtil::hasPermission($userRole, $requiredPermission)) {
//     // User has permission to read
// } else {
//     // User does not have permission to read
// }
